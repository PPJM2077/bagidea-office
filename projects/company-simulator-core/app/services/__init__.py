"""Domain services — business logic orchestration.

Each sub-module encapsulates one bounded context:
    - simulation.py    — tick engine, trigger evaluation
    - agent.py         — agent lifecycle orchestration
    - memory.py        — memory read/write coordination
    - reporting.py     — report generation
"""
